import styled from 'styled-components'

/**
 * FIXME: this is a temporary solution,
 * for this case <Text /> component should be used
 * but it isn't written yet
 */
export const ListTitle = styled.div`
  font-family: Roboto, sans-serif;
  font-size: 24px;
  font-weight: 500;
`
