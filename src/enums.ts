export enum Appearance {
  Primary = 'primary',
  Secondary = 'secondary',
}

export enum Size {
  Small = 'small',
  Medium = 'medium',
  Large = 'large',
}

export enum Variant {
  Filled = 'filled',
  Outlined = 'Outlined',
  Text = 'Text',
}
